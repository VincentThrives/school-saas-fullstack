<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- HR self-attendance geofence: the Mark IN / OUT page reads the
         user's coordinates to check they're inside the school's allowed
         radius. FINE is the "precise" option that Android surfaces
         separately at runtime — required for the 20-60 m accuracy our
         geofence enforces. COARSE lets the OS fall back to cell/wifi
         when GPS is unavailable indoors. Both permissions are prompted
         for lazily by the Capacitor Geolocation plugin on first call. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Push notifications: Android 13+ requires explicit user permission to
         show notifications. The Capacitor plugin prompts for this on first
         registration. Older versions ignore the declaration but it's
         harmless. Other FCM-related permissions (RECEIVE, WAKE_LOCK,
         VIBRATE) are auto-merged from the Firebase library manifest. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
